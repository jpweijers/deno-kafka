{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "coverage": "rm -rf coverage && deno test --allow-net --coverage && deno coverage --html"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
